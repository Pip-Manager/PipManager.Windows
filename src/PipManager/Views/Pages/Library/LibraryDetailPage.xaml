<Page
    x:Class="PipManager.Views.Pages.Library.LibraryDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lang="clr-namespace:PipManager.Languages"
    xmlns:pages="clr-namespace:PipManager.Models.Pages"
    xmlns:library="clr-namespace:PipManager.Views.Pages.Library"
    x:Name="LibraryDetail"
    Title="LibraryDetailPage"
    d:DataContext="{d:DesignInstance library:LibraryDetailPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="700"
    d:DesignWidth="1200"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid Margin="0,0,0,20">
        <TextBlock
            Margin="0,32,0,0"
            FontSize="26"
            FontWeight="Bold"
            Text="{x:Static lang:Lang.LibraryDetail_Header}" />
        <StackPanel Margin="0,80,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <ui:Badge Appearance="Primary" Content="{x:Static lang:Lang.LibraryDetail_Package}" />
                    <ui:Badge Appearance="Primary" Content="{x:Static lang:Lang.LibraryDetail_Version}" />
                    <ui:Badge Appearance="Primary" Content="{x:Static lang:Lang.LibraryDetail_Path}" />
                    <ui:Badge Appearance="Primary" Content="{x:Static lang:Lang.LibraryDetail_Summary}" />
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Margin="7,5,0,5" Text="{Binding ViewModel.Package.Name, Mode=TwoWay}" />
                    <TextBlock Margin="7,5,0,5" Text="{Binding ViewModel.Package.Version, Mode=TwoWay}" />
                    <TextBlock Margin="7,5,0,5" Text="{Binding ViewModel.Package.Path, Mode=TwoWay}" />
                    <TextBlock Margin="7,5,0,5" Text="{Binding ViewModel.Package.Summary, Mode=TwoWay}" TextWrapping="WrapWithOverflow" />
                </StackPanel>
            </Grid>
            <TextBlock
                Margin="0,10,0,0"
                FontSize="20"
                Text="{x:Static lang:Lang.LibraryDetail_Contact_Title}" />
            <Border
                Margin="0,3,0,0"
                Padding="10"
                BorderBrush="Gray"
                BorderThickness="2"
                CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="35" />
                        <RowDefinition Height="35" />
                        <RowDefinition Height="35" />
                    </Grid.RowDefinitions>
                    <StackPanel
                        Grid.Row="0"
                        VerticalAlignment="Top"
                        Orientation="Horizontal">
                        <ui:Badge Appearance="Secondary" Content="{x:Static lang:Lang.LibraryDetail_Contact_Author}" />
                        <TextBlock Margin="7,5,0,5" Text="{Binding ViewModel.Author, Mode=TwoWay}" />
                    </StackPanel>
                    <StackPanel
                        Grid.Row="1"
                        VerticalAlignment="Top"
                        Orientation="Horizontal">
                        <ui:Badge Appearance="Secondary" Content="{x:Static lang:Lang.LibraryDetail_Contact_AuthorEmail}" />
                        <TextBlock
                            Margin="7,5,0,5"
                            Text="{Binding ViewModel.AuthorEmail, Mode=TwoWay}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel
                        Grid.Row="2"
                        VerticalAlignment="Top"
                        Orientation="Horizontal">
                        <ui:Badge Appearance="Secondary" Content="{x:Static lang:Lang.LibraryDetail_Contact_ProjectUrl}" />
                        <ItemsControl
                            Margin="5,0,0,0"
                            HorizontalAlignment="Left"
                            ItemsSource="{Binding ViewModel.ProjectUrl}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel HorizontalAlignment="Stretch" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:HyperlinkButton
                                            Content="{Binding UrlType}"
                                            Icon="{Binding Icon}"
                                            NavigateUri="{Binding Url}"
                                            ToolTip="{Binding Url}"
                                            Visibility="{Binding Url, Converter={StaticResource NullToVisibility}}" />
                                        <TextBlock
                                            Margin="3,5,0,5"
                                            Text="{x:Static lang:Lang.LibraryDetail_Unknown}"
                                            Visibility="{Binding Url, Converter={StaticResource NotNullToVisibility}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>
            <TextBlock
                Margin="0,10,0,0"
                FontSize="20"
                Text="{x:Static lang:Lang.LibraryDetail_Classifier_Title}" />
            <Border
                Margin="0,3,0,0"
                Padding="10"
                BorderBrush="Gray"
                BorderThickness="2"
                CornerRadius="5">
                <StackPanel>
                    <TextBlock Margin="0,0,0,5" Text="{x:Static lang:Lang.LibraryDetail_Classifier_DevelopmentStatus}" />
                    <ui:Badge
                        HorizontalAlignment="Left"
                        Appearance="{Binding ViewModel.DevelopmentStatusAppearance}"
                        Content="{Binding ViewModel.DevelopmentStatus}" />
                    <TextBlock Margin="0,5,0,5" Text="{x:Static lang:Lang.LibraryDetail_Classifier_ProgrammingLanguage}" />
                    <ItemsControl HorizontalAlignment="Left" ItemsSource="{Binding ViewModel.ProgrammingLanguage}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Stretch" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ui:Badge Content="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Margin="0,5,0,5" Text="{x:Static lang:Lang.LibraryDetail_Classifier_IntendedAudience}" />
                    <ItemsControl HorizontalAlignment="Left" ItemsSource="{Binding ViewModel.IntendedAudience}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Stretch" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ui:Badge Content="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Margin="0,5,0,5" Text="{x:Static lang:Lang.LibraryDetail_Classifier_OperatingSystem}" />
                    <ItemsControl HorizontalAlignment="Left" ItemsSource="{Binding ViewModel.OperatingSystem}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Stretch" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ui:Badge Content="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Margin="0,5,0,5" Text="{x:Static lang:Lang.LibraryDetail_Classifier_Environment}" />
                    <ItemsControl HorizontalAlignment="Left" ItemsSource="{Binding ViewModel.Environment}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Stretch" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ui:Badge Content="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Margin="0,5,0,5" Text="{x:Static lang:Lang.LibraryDetail_Classifier_Topic}" />
                    <ItemsControl HorizontalAlignment="Left" ItemsSource="{Binding ViewModel.Topic}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Stretch" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ui:Badge Content="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>