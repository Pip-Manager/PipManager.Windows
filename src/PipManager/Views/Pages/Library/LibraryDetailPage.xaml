<Page
    x:Class="PipManager.Views.Pages.Library.LibraryDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lang="clr-namespace:PipManager.Languages"
    xmlns:library="clr-namespace:PipManager.Views.Pages.Library"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:PipManager.Models.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="LibraryDetail"
    Title="LibraryDetailPage"
    d:DataContext="{d:DesignInstance library:LibraryDetailPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="700"
    d:DesignWidth="1200"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid>
        <TextBlock
            Margin="0,32,0,0"
            FontSize="26"
            FontWeight="Bold"
            Text="{x:Static lang:Lang.LibraryDetail_Header}" />
        <Grid Margin="0,80,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <ui:Badge Appearance="Primary" Content="Package" />
                <ui:Badge Appearance="Primary" Content="Version" />
                <ui:Badge Appearance="Primary" Content="Path" />
                <ui:Badge Appearance="Primary" Content="Summary" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Margin="7, 5, 0, 5" Text="{Binding ViewModel.Package.Name, Mode=TwoWay}" />
                <TextBlock Margin="7, 5, 0, 5" Text="{Binding ViewModel.Package.Version, Mode=TwoWay}" />
                <TextBlock Margin="7, 5, 0, 5" Text="{Binding ViewModel.Package.Path, Mode=TwoWay}" />
                <TextBlock Margin="7, 5, 0, 5" Text="{Binding ViewModel.Package.Summary, Mode=TwoWay}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>