<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:library="clr-namespace:PipManager.Controls.Library"
    xmlns:lang="clr-namespace:PipManager.Languages"
    xmlns:pages="clr-namespace:PipManager.Models.Pages"
    xmlns:valueConverters="clr-namespace:ValueConverters;assembly=ValueConverters">
    <Grid x:Key="LibraryDeletionWarningMsgBoxContent" VerticalAlignment="Top">
        <TextBlock
            VerticalAlignment="Top"
            FontSize="16"
            Foreground="Red"
            Text="{I18N {x:Static lang:LangKeys.Msgbox_Message_LibraryDeletionWarning}}" />
        <ScrollViewer Margin="0,30,0,0" VerticalAlignment="Top">
            <ItemsControl x:Name="LibraryDeletionWarningMsgBoxContentListView">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type pages:LibraryListItem}">
                        <Grid>
                            <StackPanel Margin="7,0,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Text="{Binding PackageName}" />
                                    <TextBlock FontSize="14" Text=" " />
                                    <ui:Badge
                                        Appearance="Primary"
                                        Content="{Binding PackageVersion}"
                                        FontSize="12" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>

    <Grid x:Key="LibraryCheckUpdateMsgBoxContent" VerticalAlignment="Top">
        <Grid.Resources>
            <valueConverters:BoolToVisibilityConverter x:Key="BoolToVisibility" FalseValue="Collapsed" />
            <valueConverters:ValueConverterGroup x:Key="InverseBoolToVisibility">
                <valueConverters:InverseBoolConverter />
                <valueConverters:BoolToVisibilityConverter FalseValue="Collapsed" />
            </valueConverters:ValueConverterGroup>
        </Grid.Resources>
        <TextBlock
            VerticalAlignment="Top"
            FontSize="16"
            Foreground="OrangeRed"
            Text="{I18N {x:Static lang:LangKeys.MsgBox_Message_FindUpdate}}"
            Visibility="Collapsed" />
        <TextBlock
            VerticalAlignment="Top"
            FontSize="16"
            Text="{I18N {x:Static lang:LangKeys.MsgBox_Message_PackageIsLatest}}"
            Visibility="Collapsed" />
        <ScrollViewer Margin="0,30,0,0" VerticalAlignment="Top">
            <ItemsControl x:Name="LibraryCheckUpdateMsgBoxContentListView">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type library:LibraryCheckUpdateMsgBoxContentListItem}">
                        <Grid>
                            <StackPanel Margin="7,0,0,0" Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="TabDesktopNewPage20" Visibility="{Binding NeedUpdate, Converter={StaticResource InverseBoolToVisibility}}" />
                                <ui:SymbolIcon
                                    Foreground="LimeGreen"
                                    Symbol="ArrowCircleUp24"
                                    Visibility="{Binding NeedUpdate, Converter={StaticResource BoolToVisibility}}" />
                                <TextBlock
                                    Margin="3,0,0,0"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Text="{Binding PackageName}" />
                                <TextBlock FontSize="14" Text=" " />
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                <ui:Badge
                                    Appearance="Primary"
                                    Content="{Binding PackageVersion}"
                                    FontSize="12" />
                                <ui:Badge
                                    Appearance="Success"
                                    Content="{Binding NewVersion}"
                                    FontSize="12"
                                    Visibility="{Binding NeedUpdate, Converter={StaticResource BoolToVisibility}}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</ResourceDictionary>